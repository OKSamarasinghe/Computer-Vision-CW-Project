# Pin roughly to the versions confirmed working in the Kaggle notebooks (see each
# Notebook*_Findings_Summary.md "Environment" table) so local inference behaves identically.
tensorflow==2.20.0
opencv-python-headless>=4.10
numpy>=2.0
pandas>=2.3
streamlit>=1.38
reportlab>=4.2
Pillow>=10.4
matplotlib>=3.9
pytest>=8.0
