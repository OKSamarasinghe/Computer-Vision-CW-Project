# Code screenshots

Screenshots of notebook cells (code + their printed [CELL N] outputs) and of the app's source files,
used as report evidence. Name them so they're easy to reference, e.g.
`notebook4_cell7_architecture_definition.png`, `app_gradcam_module.png`.
