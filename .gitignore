# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# --- Jupyter ---
.ipynb_checkpoints/

# --- OS ---
.DS_Store
Thumbs.db

# --- Streamlit ---
.streamlit/

# --- Large trained model files: track with Git LFS instead (see top-level README) ---
# Comment these two lines out once `git lfs track "*.keras" "*.h5"` is set up, so the
# files are tracked via LFS rather than skipped entirely.
app/models/*.keras
app/models/*.h5

# --- Local scratch / secrets ---
*.env
.env
