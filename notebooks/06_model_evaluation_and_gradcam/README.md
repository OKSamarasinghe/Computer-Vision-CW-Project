# Notebook 6 — Model Evaluation & Grad-CAM (Criteria 6 & 9, 15 + 5 marks)

Place the executed notebook here, with outputs attached:

- `Notebook6_Model_Evaluation_and_Grad-CAM.ipynb`

Covers: test-set accuracy/precision/recall/F1/confusion matrix/QWK, error analysis, and the
mandatory Grad-CAM innovation feature (`generate_gradcam_heatmap`, `overlay_gradcam_heatmap_on_image`
— both ported directly into `../../app/backend/gradcam.py` for the local application).

Matching Kaggle outputs (metrics CSV, confusion matrix figure, error-analysis CSV, Grad-CAM overlay
grids, metadata JSON) go in `../../kaggle_artifacts/notebook_6_evaluation_gradcam/`.
