# Notebook 3 — Augmentation & Balancing (Criterion 3, 10 marks)

Place the executed notebook here, with outputs attached:

- `Notebook3_Augmentation_and_Balancing.ipynb`

Covers: rotation/flip/zoom/brightness augmentation, capped per-class augmentation, and class-weight
computation used to counter the 9.35× imbalance found in Notebook 1.

Matching Kaggle outputs (augmented training manifest CSV, class-weights pickle, before/after class
distribution chart) go in `../../kaggle_artifacts/notebook_3_augmentation/`.
